# --- Dependencies / package managers ---
node_modules/
.pnp
.pnp.*

# Yarn Berry (keep only these subfolders)
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# pnpm / npm logs
pnpm-debug.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# --- Testing ---
coverage/

# --- Next.js ---
.next/               # build output (server build)
out/                 # export output (we're not using it, but ignore if generated)

# --- Build artifacts ---
build/
dist/

# --- Env files ---
.env*
!.env.example

# --- Vercel (local project config) ---
.vercel/

# --- TypeScript ---
*.tsbuildinfo
# (keep next-env.d.ts committed)

# --- Misc ---
.DS_Store
*.pem
.next
